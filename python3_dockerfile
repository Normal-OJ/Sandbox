# for python3

FROM python:3.7.5-alpine3.9

MAINTAINER Normal-OJ

# install seccomp
RUN apk add --no-cache libseccomp

# sandbox user with uid:1450 home:/sandbox D:no passwd
RUN adduser sandbox -u 1450 -D

# result folder
RUN mkdir /result

# sandbox binary
COPY sandbox /usr/bin/