# for c_cpp

FROM alpine

MAINTAINER Normal-OJ

# install g++
RUN apk add --no-cache g++

# sandbox user with uid:1450 D:no passwd
RUN adduser sandbox -u 1450 -D

# result folder
RUN mkdir /result

# sandbox binary
COPY sandbox /usr/bin/

